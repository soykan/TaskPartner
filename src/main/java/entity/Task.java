package entity;

import javax.persistence.Entity;


@Entity
public class Task {
	private Long id;
	private String name;
	/*
	 * Add 
	 * 		duration, 
	 * 		start time, 
	 * 		end time,
	 * 		user (foreign key)
	 * Here
	 */
}
